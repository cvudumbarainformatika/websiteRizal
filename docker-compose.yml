version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: master-rizal-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
